<% if current_user.avatar_url.nil? %>
    <%= link_to image_tag(current_user.gravatar_url, width: 80, class: 'img-rounded'), current_user %>
<% elsif !current_user.nil? %>
    <%= link_to image_tag(current_user.avatar, width: 80, class: 'img-rounded'), current_user %>
<% end %>
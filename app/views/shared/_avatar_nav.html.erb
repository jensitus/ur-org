<% if current_user.avatar_url.nil? %>
    <%= image_tag current_user.gravatar_url, width: 40, class: 'img-rounded' %>
<% elsif !current_user.nil? %>
    <%= image_tag current_user.avatar, width: 40, class: 'img-rounded' %>
<% end %>
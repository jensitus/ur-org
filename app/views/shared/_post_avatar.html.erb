<% if @user.avatar_url.nil? %>
    <%= link_to image_tag(@user.gravatar_url, width: 80, class: 'img-rounded'), current_user %>
<% elsif !current_user.nil? %>
    <%= link_to image_tag(@user.avatar, width: 80, class: 'img-rounded'), current_user %>
<% end %>
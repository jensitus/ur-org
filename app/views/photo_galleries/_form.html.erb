<div class="row">
  <div class="col-md-6">
    <%= form_for(@photo_gallery, html: {multipart: true}) do |f| %>
        <%# if @photo_gallery.errors.any? %>
            <!--<div id="error_explanation">
              <%#= pluralize(@photo_gallery.errors.count, 'error') %> prohibited this gallery from being saved:

                <%#= @photo_gallery.errors.inspect %>
                <%# @photo_gallery.errors.full_messages.each do |message| %>

                <%# end %>

            </div>-->


        <%# end %>

        <div class="form-group">
          <%= f.text_field :title, class: 'form-control', placeholder: 'Title must not be blank' %>
        </div>
        <div class="form-group">
          <%= f.text_area :description, class: 'form-control', placeholder: 'Description' %>
        </div>
        <%#= f.file_field :picture, multiple: true, name: 'photos[picture][]' %>
        <%= render 'shared/upload_button' %>

        <div class="form-group">
          <%= f.submit 'submit this', class: 'btn btn-default' %>
        </div>
    <% end %>
  </div>
</div>


<script>
  window.onload = init();
</script>
